import { Typography } from '@mui/material';
import React, { FC } from 'react';

interface HeaderProps {}

const Header: FC<HeaderProps> = () => <Typography variant="subtitle1">Header works</Typography>;

export default Header;
